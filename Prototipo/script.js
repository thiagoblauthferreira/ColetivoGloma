$(document).ready(function() {
    $('#userForm').submit(function(event) {
        event.preventDefault();
        // Coloque aqui seu código de envio de formulário via AJAX ou qualquer outra lógica de manipulação de dados
    });
});
